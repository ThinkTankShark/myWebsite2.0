- if controller_name == 'home' || controller_name == 'blogs' && params[:action] != ('new')
  = navbar position: :static do
    = nav class: 'navbar-right' do
      = render partial: "application/account_dropdown"
